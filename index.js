// index.js
const head   = require('./head');
const tail   = require('./tail');
const middle = require('./middle');
const findKey = require('./findKey')
const map.js = require('/map')

module.exports = {
  head:   head,
  tail:   tail,
  middle: middle,
  findKey: findKey,
  map: map
};
